# electron-cmdlove

This app creates tray icon with CMD commands tied to it.
Right click the icon and run cmd command directly.

By default the app starts ConEmu from `C:\Program Files\ConEmu\ConEmu64.exe`.
Set the `CMDLOVE_CONEMU_PATH` environment variable to use a different ConEmu executable path.
